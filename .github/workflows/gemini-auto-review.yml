name: Gemini Auto PR Review

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  gemini-review:
    if: ${{ github.event.pull_request.head.repo.fork == false && github.event.pull_request.head.repo.full_name == github.repository }}
    permissions:
      actions: read
      contents: read
      issues: write
      pull-requests: write
    uses: jhw7500/automation/.github/workflows/gemini-auto-review.yml@b79d282d25c04bcdb63c26e47773d3c86fdf60a3
    with:
      pr_number: ${{ github.event.pull_request.number }}
      repo_write_auth: github_token
      publisher_app_id: ${{ vars.APP_ID }}
    secrets:
      GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
